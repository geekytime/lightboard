{
  "name": "lightboard",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:geekytime/lightboard.git",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build --stream",
    "lint": "lerna run lint --stream",
    "push": "lerna run push --stream",
    "validate": "lerna run validate --stream"
  },
  "devDependencies": {
    "lerna": "^3.13.1"
  }
}
